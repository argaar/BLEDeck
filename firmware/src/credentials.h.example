#pragma once

// Copy this file to credentials.h and fill in your values.
// credentials.h is gitignored - never commit it.

#define OTA_WIFI_SSID      "your_wifi_ssid"
#define OTA_WIFI_PASSWORD  "your_wifi_password"

// HTTP Basic auth password for the /update upload page.
// This stays static so you can flash from any browser.
// The AP fallback password is generated randomly at runtime and shown on the OLED.
#define OTA_HTTP_PASSWORD  "your_ota_http_password"
