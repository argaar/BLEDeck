-r requirements.txt
# pytest pinned <9 because pytest-asyncio 0.26 caps pytest below 9.0.
# Remove the cap once pytest-asyncio bumps. Reality-tracking pin, not aspirational.
pytest>=8.3,<9
pytest-asyncio>=0.24,<1
pytest-benchmark>=4,<6
